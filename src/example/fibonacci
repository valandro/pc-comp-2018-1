int fibonacci(int n) {
  if (n == 0) then {
    return 1;
  };
  
  if (n == 1) then {
    return 1;
  };
  
  return fibonacci(n - 1) + fibonacci(n - 2);
}

int main()
{
  int numero;
  numero = 5;

  return fibonacci(numero);
}
