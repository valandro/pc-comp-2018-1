/* parameter passing: 49 in the bss address */
int x;

int f(int z, int w)
{
  return z*w;
}

int mult (int z, int w, int a, int b)
{
  int x;
  x = f(z,w)+f(a,b);
  return x;
}

int main()
{
  int y;
  int z;
  y = 7;
  z = 3;
  x = mult(y, y, z, z);
}
