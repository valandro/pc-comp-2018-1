int x;
int K;

int f(int z, int w)
{
  return z*w;
}

int mult (int z, int w, int a, int b)
{
  int x;
  x = f(f(z,z),w)+f(a,f(b,b));
  return x;
}

int main()
{
  int y;
  int z;
  y = 2;
  z = 2;
  K = mult(y*3, y*4, z*2, z*2);
}
